engines:
  duplication:
    enabled: true
    config:
      languages:
        - ruby
  rubocop:
    enabled: true
ratings:
  paths:
  - "app/**"
  - "lib/**"
  - Gemfile.lock
exclude_paths:
  - "spec/*"
